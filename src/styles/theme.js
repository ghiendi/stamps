// Ant Design theme configuration
const themeConfig = {
  token: {
    borderRadius: 3,
    boxShadow: 'none',
    fontFamily: "'Maven Pro', Arial, Helvetica, sans-serif",
  },
  components: {
    Button: { boxShadow: 'none' },
    Card: { boxShadow: 'none', borderRadius: 3 },
    Input: { boxShadow: 'none', borderRadius: 3 },
    Menu: { boxShadow: 'none', borderRadius: 3 },
    Layout: { boxShadow: 'none', borderRadius: 3 },
  },
};

export default themeConfig;
